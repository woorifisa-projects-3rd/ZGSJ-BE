server:
  port: 7777

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/jihyuk0414/first-team-config-server.git
          clone-on-start: true
          default-label: main

